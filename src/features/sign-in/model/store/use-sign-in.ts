import { createWithEqualityFn as create } from "zustand/traditional";

// types
import type { UseSignInStore } from "../types/use-sign-in";

export const useSignIn = create<UseSignInStore>(() => ({
  
}))